#include "main.h"
/**
 * add - adss two integers together
 * @n1: being added to n2.
 * @n2: being added to n1.
 * Return: this returns the result
 */
int add(int n1, int n2)
{
int result = n1 + n2;
return (result);
}
