#include "main.h"
/**
 * mul - multiplys two numbers together
 * @a: is being multiplied
 * @b: is being multiplied
 * Return: returns c
 */
int mul(int a, int b)
{
int c;
c = a * b;
return (c);
}
  
