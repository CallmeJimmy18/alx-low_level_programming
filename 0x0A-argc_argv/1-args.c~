#include <stdio.h>
#include "main.h"
/**
 * main - prints the number of arguments passed into it.
 * @argc: number of arguments
 * Return: Always 0
 */
int main(int argc)
{
  printf("%d\n", argc - 1);
  return (0);
}
